# Optional: publish web on host loopback for rare host-network peers.
# Usage: docker compose -f docker-compose.yml -f docker-compose.host-port.example.yml up -d
services:
  web:
    ports:
      - "127.0.0.1:8000:8000"
